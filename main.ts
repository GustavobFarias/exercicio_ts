function multiplicar(n1: number, n2: number): number {
    return n1 * n2;
}

console.log(multiplicar(5, 5));

function dizOla(nome: string): string {
    return `Olá ${nome}`
}

console.log(dizOla("Gustavo"));